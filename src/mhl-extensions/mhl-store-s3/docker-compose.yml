# Local S3 for developing / testing the mhl-store-s3 extension.
#
#   docker compose up -d          # MinIO on :9000 (API) and :9001 (console)
#   docker compose down -v        # stop and wipe the data volume
#
# Dev credentials (do NOT reuse anywhere real):
#   access key id     = mhl
#   secret access key = mhl-secret-key
#   bucket            = mhl-state
#   endpoint          = http://localhost:9000
name: mhl-store-s3

services:
  minio:
    image: minio/minio:RELEASE.2024-06-13T22-53-53Z
    command: server /data --console-address ":9001"
    environment:
      MINIO_ROOT_USER: mhl
      MINIO_ROOT_PASSWORD: mhl-secret-key
    ports:
      - "9000:9000"
      - "9001:9001"
    volumes:
      - minio-data:/data

  # One-shot: wait for MinIO, then create the bucket the extension expects.
  # `mc`'s `until` loop is the readiness gate — the server image ships no
  # curl/healthcheck tool of its own.
  createbucket:
    image: minio/mc:RELEASE.2024-06-12T14-34-03Z
    depends_on:
      minio:
        condition: service_started
    entrypoint: >
      /bin/sh -c "
      until mc alias set local http://minio:9000 mhl mhl-secret-key; do
        echo 'waiting for minio...'; sleep 1;
      done &&
      mc mb --ignore-existing local/mhl-state &&
      mc anonymous set none local/mhl-state &&
      echo 'bucket mhl-state ready'
      "

volumes:
  minio-data:
