# Local Redis for developing / testing the mhl-store-redis extension.
#
#   docker compose up -d --wait     # Redis 7 on host :6381 -> container 6379
#   docker compose down -v          # stop and wipe
#
#   url = redis://localhost:6381/0
name: mhl-store-redis

services:
  redis:
    image: redis:7-alpine
    command: ["redis-server", "--save", "", "--appendonly", "no"]
    ports:
      - "6381:6379"
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 2s
      timeout: 3s
      retries: 20
