# IAO folders

.claude/
.codex/
.harness/
.kimi/

# files
harness*.json
.env

# folders
specs/
dotnet/
/output/
/tmp
/dist/
/out/
logs/
logs-*/

# build artifacts (built by release.yml on tag push, not committed)
*.vsix
.DS_Store

# mhl build artifacts
/mhl
/mhl.exe
.mhl/
sample/mhl
*.nosync/

# tests_e2e/*/mhl and per-scenario copies are built locally/by CI from
# src/mhl-runtime (see each run-all.sh's pre-flight hint) — never commit a
# binary here, it goes stale against the actual runtime immediately.
tests_e2e/**/mhl
tests_e2e/**/mhl.exe
tests_e2e/**/bin/


# scripts
release.sh
PLANO.md


