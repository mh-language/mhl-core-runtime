# conditional_symbols

The comparison operators: `<`, `>`, `<=`, `>=`, `==`, `!=`.

- [conditional_symbols.mh](conditional_symbols.mh) — every comparison operator evaluated
  against the same pair of numbers
