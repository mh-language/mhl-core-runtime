agent Local {
    command: "echo"
    args: ["resposta local"]
}

pipeline Hello {
    input name: string

    step Greet {
        var message = "Ola, ${name}!"
        log(message)
        var answer = Local.run(prompt: message)
        log(answer)
    }
}
